<%= form_for(@site_manager) do |f| %>
  <% if @site_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_manager.errors.count, "error") %> prohibited this site_manager from being saved:</h2>

      <ul>
      <% @site_manager.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url, remote: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
